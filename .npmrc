@wyre-ai:registry=https://npm.pkg.github.com
# Transition compat: SDK deps stay on the legacy scope until the
# @wyre-ai/node-* packages are flipped public (UI-only) and deps re-flipped.
@wyre-technology:registry=https://npm.pkg.github.com
